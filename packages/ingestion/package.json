{
  "name": "@agentlens/ingestion",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "gateway": "pnpm build && node dist/gateway/server.js",
    "writer": "pnpm build && node dist/writer/main.js",
    "partition-job": "pnpm build && node dist/writer/partition-job.js",
    "loadtest": "node loadtest/run.ts",
    "crashtest": "node loadtest/crash.ts"
  },
  "dependencies": {
    "@agentlens/shared": "workspace:*",
    "fastify": "^5.10.0",
    "ioredis": "^5.11.1",
    "pg": "^8.22.0",
    "pg-copy-streams": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@types/pg": "^8.11.10",
    "@types/pg-copy-streams": "^1.2.5"
  }
}
